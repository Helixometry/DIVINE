[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "divine-lib"
version = "0.1.0"
description = "Reusable two-modality DIVINE backbone library"
authors = [{name = "Your Name"}]
dependencies = ["torch"]
